<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MyLauncher" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Custom Launcher Theme Attributes -->
        <item name="launcherBackgroundColor">@color/light_background</item>
        <item name="cardBackgroundColor">@color/light_card_background</item>
        <item name="launcherTextColorPrimary">@color/light_text_primary</item>
        <item name="launcherIconTint">@color/light_icon_tint</item>
        <item name="searchBarBackgroundColor">@color/light_search_bar_background</item>
        <item name="searchBarHintColor">#8A000000</item> <!-- 54% black for hint text -->

        <!-- You can also map to standard material attributes if desired -->
        <!-- <item name="android:windowBackground">?attr/launcherBackgroundColor</item> -->
        <!-- <item name="android:textColorPrimary">?attr/launcherTextColorPrimary</item> -->
    </style>

    <style name="Theme.MyLauncher" parent="Base.Theme.MyLauncher" />
</resources>